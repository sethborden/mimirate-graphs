# mimirate-graphs

Some d3 charts that were built for use with Mimirate's cashflow/burn rate calculator. `npm-install && lite-server` to get things up and running.  The first chart requires the output from [mimirate-calc](https://github.com/sethborden/mimirate-calc) to really shine.
